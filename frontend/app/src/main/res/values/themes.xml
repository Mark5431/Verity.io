<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VhackWallet" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme for API 24-30. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Changed to the wrapper drawable to fix distortion -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.VhackWallet</item>
    </style>
</resources>
